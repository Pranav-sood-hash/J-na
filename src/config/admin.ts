/**
 * Admin Configuration
 * 
 * Contains default admin credentials for development/setup.
 * In production, these should be managed through environment variables.
 * 
 * NOTE: The admin account must be created in Supabase with these credentials,
 * and the user must be assigned the 'admin' role in the user_roles table
 * for access to the admin dashboard.
 */

export const ADMIN_CONFIG = {
  email: 'soodpranav235@gmail.com',
  password: 'par@&006',
};
